import UIKit

class StickersTableViewController: UITableViewController {

    override func viewDidLoad() {
        super.viewDidLoad()
        // Giao diện đã có Storyboard lo, code này để trống là chuẩn bài!
    }

    // TUYỆT ĐỐI KHÔNG VIẾT numberOfSections hay numberOfRowsInSection Ở ĐÂY
}
